[theme]
# ベースを「白（ライトモード）」に固定する
base = "light"

# メインカラー（ボタンや強調部分の色）
# 好きな色に変えられます（例：緑=#2E8B57, 青=#1E90FF, オレンジ=#FF8C00）
primaryColor = "#2E8B57" 

# 背景色（基本は白でOK）
backgroundColor = "#FFFFFF"

# 補助的な背景色（サイドバーなどの色）
secondaryBackgroundColor = "#F0F2F6"

# 文字の色
textColor = "#262730"
